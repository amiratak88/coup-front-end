import React from 'react'

const ChallengePrompt = (props) => (
	<div>
		<p>Challenge this player?</p>
		<button onClick={ "" /* handleClick destructed from props */ }>Challenge</button>
		<button onClick={ "" /* handleClick destructed from props */ }>Allow</button>
	</div>
)

export default ChallengePrompt